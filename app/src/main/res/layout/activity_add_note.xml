<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_add_reflection"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background_generic"
    android:orientation="vertical"
    tools:context="com.example.emily.a362first.AddNote">

    <EditText
        android:id="@+id/enter_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ems="10"
        android:hint="Enter Title"
        android:inputType="textPersonName"
        android:paddingEnd="10dp"
        android:paddingStart="10dp" />

    <EditText
        android:id="@+id/enter_content"
        android:layout_width="match_parent"
        android:layout_height="400dp"
        android:ems="10"
        android:gravity="top"
        android:hint="@string/enter_content"
        android:inputType="textMultiLine"
        android:paddingEnd="10dp"
        android:paddingStart="10dp" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="50dp"
        android:layout_gravity="end"
        android:background="@drawable/rectangle_button_transparent"
        android:onClick="saveMessage"
        android:text="@string/button_save" />

</LinearLayout>

